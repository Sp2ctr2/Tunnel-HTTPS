# Release notes draft

- Improved shutdown ordering and connection-state accuracy.
- Added bounded configuration validation and safe browser-only behavior.
- Added explicit DNS overload responses and strengthened DNS response validation tests.
- Corrected FAST IPv6 routing and split-domain runtime wiring.
- Improved TCP receive-window, FIN, and TUN-write sequencing behavior.
- Hardened local WebView resource and bridge boundaries.

Do not publish these notes until device packet-path regression tests and disclosure/signing blockers are complete.
